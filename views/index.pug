extends layout

block content
  h1= title
  p Welcome to #{title}
  ul 
    each message in messages
      li
        h2 #{message.user}
        p #{message.text}
        h4 #{message.added}

  a(href="/new") Add a new post!
